<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>BCA Solutions - Home</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

        body {
            font-family: 'Press Start 2P', cursive;
            margin: 0;
            background-color: #f0f0f0;
            overflow-x: hidden;
        }

        header {
            color: white;
            text-align: center;
            background: url('your-background-image.jpg') no-repeat center/cover;
            padding: 15px;
            position: relative;
        }

        header .header-img {
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-130%);
            height: 60px;
        }

        header h2 {
            margin: 0;
            font-size: 3em;
            background-color: rgba(255, 111, 97, 0.8);
            border-radius: 20px;
            padding: 15px;
        }

        nav ul {
            display: flex;
            justify-content: center;
            background-color: #ff6d61;
            padding: 10px;
            margin: 10px 0;
            list-style: none;
        }

        nav ul li {
            margin: 0 15px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            transition: transform 0.3s;
        }

        nav ul li:hover {
            background-color: #753838;
            border-radius: 30px;
        }

        .hero-section {
            background: linear-gradient(135deg, #ff9a9e, #ffecd2);
            text-align: center;
            padding: 50px 0;
        }

        .hero-section h2 {
            font-size: 3.5em;
            color: #ff6f61;
            animation: bounce 1.5s infinite;
        }

        .hero-section p {
            font-size: 1.2em;
            color: white;
            margin: 10px 0;
            animation: flash 2s infinite;
        }

        .btn-primary {
            background-color: #ff6f61;
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            text-decoration: none;
            transition: background-color 0.4s, transform 0.4s;
            display: inline-flex;
            align-items: center;
        }

        .btn-primary:hover {
            background-color: #ff3d3d;
            transform: scale(1.1);
        }

        .search-bar {
            display: flex;
            justify-content: center;
            margin: 24px;
        }

        .search-bar input[type="text"] {
            padding: 15px;
            border-radius: 30px;
            width: 60%;
            border: 2px solid #ff6f61;
            outline: none;
        }

        .search-bar .search-btn {
            background-color: transparent;
            border: none;
            cursor: pointer;
        }

        .button-group {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 20px;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        .features {
            display: flex;
            justify-content: space-around;
            margin: 50px 0;
        }

        .feature {
            background-color: #fff;
            border-radius: 20px;
            padding: 20px;
            text-align: center;
            transition: transform 0.3s;
            width: 100px;
        }

        .feature:hover {
            transform: scale(1.1);
        }
        .features {
            display: flex;
            justify-content: space-around;
            margin: 50px 0;
        }

        .feature {
            background-color: #fff;
            border-radius: 20px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 28%;
            text-align: center;
            transition: transform 0.3s;
        }

        .feature:hover {
            transform: rotate(-5deg) scale(1.1);
        }

        .feature img {
            width: 120px;
            height: 120px;
            margin-bottom: 15px;
        }

        .feature h3 {
            font-size: 13px;
            color: #ff6f61;
            margin-bottom: 10px;
        }



        footer {
            background-color: #ff6f61;
            color: white;
            text-align: center;
            padding: 20px 0;
        }
        .outputBox{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
    </style>
</head>
<body>
    <header>
        <img src="your-image.png" alt="Decorative Image" class="header-img">
        <h2>BCA Solutions</h2>
        <nav>
            <ul>
                <li><a href="home.html">Home</a></li>
                <li><a href="Syllabus.html">Syllabus</a></li>
                <li><a href="contact.html">Contact</a></li>
                <li><a href="aboutus.html">About Us</a></li>
            </ul>
        </nav>
    </header>

    <section class="hero-section">
        <h2>Welcome</h2>
        <p>Your One-Stop Destination for BCA Learning</p>
        <form action="/submit" method="POST">
        <div class="search-bar">
            <input type="text" placeholder="Search your problem here" name="userQuery">
            <button class="search-btn" type="submit">Submit</button>
        </div>
        </form>
        <div class="button-group">
            <a href="/question" class="btn-primary">Questions</a>
            <a href="#teachers" class="btn-primary">Short notes</a>
            <a href="#resources" class="btn-primary">Browse Resources</a>
            <a href="#testimonials" class="btn-primary">Quick Solutions</a>
        </div>

        <div class="outputBox">
            <%if(userResponse){%>
                <h1><%=userResponse%></h1>
                <%}%>
        </div>

    </section>
    <section class="features">
        <div class="feature">
            <img width="200px" src="Teacher.jpg" alt="Feature 1">
            <h3>Expert Teachers</h3>
            <p>Learn from top educators with years of experience.</p>
        </div>
        <div class="feature">
            <img src="Laptop.png" alt="Feature 2">
            <h3>Comprehensive Courses</h3>
            <p>All the subjects and materials you need to excel in BCA.</p>
        </div>
        <div class="feature">
            <img src="image.png" alt="Feature 3">
            <h3>Interactive Learning</h3>
            <p>Engage with peers and teachers through live sessions.</p>
        </div>
    </section>


    

    <footer>
        <p>&copy; 2024 BCA Solutions | All Rights Reserved</p>
    </footer>
</body>
</html>
